import { useState } from 'react';
import { Button } from 'components';
import Modal from './index';

<Meta
  title="Molecule/Modal"
  component={Modal}
  parameters={{ status: { type: 'alpha' } }}
  args={{
    children: (
      <p>
        Lorem ipsum dolor sit amet, aliquip sit commodo est esse esse tempor
        sunt velit incididunt nostrud in ut sit. Qui dolore consequat deserunt
        laborum sunt ipsum in et dolor dolore ex Lorem commodo sit. Ipsum do in
        enim qui mollit velit do est nisi proident incididunt dolore consectetur
        qui. Cupidatat ipsum pariatur aute est fugiat eu commodo deserunt
        proident tempor. Proident ut est est Lorem exercitation ex id aliqua.
      </p>
    )
  }}
/>

# Modal

The Modal component is a simple container component which can display anything in a modal window (i.e. hovers over the screen).

<Tip type="note">
  The 'blackout' and 'wrapper' sections cannot be restyled. The custom styling
  will only apply to the 'content' sections.
</Tip>

<Canvas withSource="open">
  <Story name="Default">
    {(args) => {
      const [isOpen, setIsOpen] = useState(false);
      return (
        <>
          <Button onClick={() => setIsOpen(true)}>Open Modal</Button>
          <Modal isOpen={isOpen} setIsOpen={setIsOpen} {...args} />
        </>
      );
    }}
  </Story>
</Canvas>

## Props

<ArgsTable of={Modal} />
