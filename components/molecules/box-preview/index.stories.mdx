import { BoxPreview } from "./index.tsx";

<Meta
  title="Molecule/Box preview"
  component={BoxPreview}
  args={{
      boxID: "123",
      boxNumber: 1,
      room,
      items: itemList,
      itemCount: 33,
      percentFilled: 20
    }}
/>

export const itemList = [
  {
    name: "Item 1",
  },
  {
    name: "Item 2",
    quantity: 4
  },
  {
    name: "Item 3",
  },
  {
    name: "Item 4"
  }
]

export const room = {
  name: "Kitchen",
  colour: "red",
}

<Title type="ALPHA">Box Preview</Title>

<Description of={BoxPreview} />

<Canvas withSource="open">
  <Story name="Default">
    {
      (args) => <BoxPreview {...args} />
    }
  </Story>
</Canvas>

When a box is sealed, a 'sealed' banner is displayed. When a box is sealed, the box becomes draggable and can be moved to another site.

<Canvas withSource="open">
  <Story
    name="Sealed"
    args={{
      sealed: true
    }}
  >{
    (args) => <BoxPreview {...args} />
  }
  </Story>
</Canvas>

This shows a box in a 'selected' state. Selected boxes can be moved to another site and can be dragged en masse.

<Canvas withSource="open">
  <Story
    name="Selected"
    args={{
      sealed: true,
      selected: true
    }}
  >{
    (args) => <BoxPreview {...args} />
  }
  </Story>
</Canvas>

This box is draggable. It can be moved to another site.

<Canvas withSource="open">
  <Story
    name="Draggable"
    args={{
      draggable: true
    }}
  >{
    (args) => <BoxPreview {...args} />
  }
  </Story>
</Canvas>

## Props

<ArgsTable of={BoxPreview} />
