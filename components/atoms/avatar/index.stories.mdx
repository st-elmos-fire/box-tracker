import Avatar from './index';

export const user = {
  name: 'John Doe',
  avatar:
    'https://lh3.googleusercontent.com/-XdUIqdMkCWA/AAAAAAAAAAI/AAAAAAAAAAA/4252rscbv5M/photo.jpg'
};

<Meta
  title="Atom/Avatar"
  component={Avatar}
  parameters={{ status: { type: 'beta' } }}
/>

# Avatar

The 'Avatar' component is used to display a user's profile picture. If no image
is provided then a users initials will be displayed.

If the image is too large then it will be scaled down to fit within the
component's bounds.

If no user is passed to the component then it will display a default image.

export const Template = (args) => (
  <div style={{ height: '200px' }}>
    <Avatar {...args} />
  </div>
);

<Canvas withSource="open">
  <Story
    name="Name only"
    args={{
      name: user.name,
      colour: '#6699cc'
    }}
  >
    {Template.bind({})}
  </Story>
  <Story
    name="Long name"
    args={{
      name: 'Adolph Blaine Charles David Earl Frederick Gerald Hubert Irvin John Kenneth Lloyd Martin Nero Oliver Paul Quincy Randolph Sherman Thomas Uncas Victor William Xerxes Yancy Zeus',
      colour: '#7d32a8'
    }}
  >
    {Template.bind({})}
  </Story>
  <Story
    name="Three Word Name"
    args={{
      name: 'Adolph Blaine Charles',
      colour: '#2233dd'
    }}
  >
    {Template.bind({})}
  </Story>
  <Story
    name="Single Word Name"
    args={{
      name: 'Madonna',
      colour: '#32a852'
    }}
  >
    {Template.bind({})}
  </Story>
  <Story
    name="Name with international characters"
    args={{
      name: '习近平',
      colour: '#a89c32'
    }}
  >
    {Template.bind({})}
  </Story>
  <Story name="Unassigned">{Template.bind({})}</Story>
  <Story
    name="Assigned with image"
    args={{
      imagePath: user.avatar
    }}
  >
    {Template.bind({})}
  </Story>
  <Story
    name="Assigned with image (fixed size)"
    args={{
      imagePath: user.avatar,
      size: '150px'
    }}
  >
    {Template.bind({})}
  </Story>
</Canvas>

## Props

<ArgsTable of={Avatar} />
