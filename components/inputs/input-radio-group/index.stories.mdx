import InputRadioGroup from './index';

import options from 'lib/mocks/options';

<Meta
  title="Inputs/InputRadioGroup"
  component={InputRadioGroup}
  args={{
    options,
    name: 'form-group'
  }}
/>

export const Template = (args) => <InputRadioGroup {...args} />;

# InputRadioGroup

<WorksWith component="InputFactory" />

The InputRadioGroup component groups a collection of [InputRadio](?path=/docs/atom-inputradio--unchecked) components,
and provides them all with the same name, creating a radio group.

<Canvas withSource="open">
  <Story name="Default">{Template.bind({})}</Story>
</Canvas>

Pre-checked option

<Canvas withSource="open">
  <Story
    name="With checked option"
    args={{
      options: [
        { label: '1-3 Research stage', checked: true },
        { label: '4-6 Development stage' },
        { label: '7-9 Deployment stage' }
      ],
      name: 'form-group-checked'
    }}
  >
    {Template.bind({})}
  </Story>
</Canvas>

Disabled option

<Canvas withSource="open">
  <Story
    name="With disabled option"
    args={{
      options: [
        { label: '1-3 Research stage', disabled: true },
        { label: '4-6 Development stage' },
        { label: '7-9 Deployment stage' }
      ],
      name: 'form-group-disabled'
    }}
  >
    {Template.bind({})}
  </Story>
</Canvas>

By default each radio will use the label as the value, but you can also provide custom values for each radio.

<Canvas withSource="open">
  <Story
    name="With custom values"
    args={{
      options: [
        { label: '1-3 Research stage', value: '1' },
        { label: '4-6 Development stage', value: '2' },
        { label: '7-9 Deployment stage', value: '3' }
      ],
      name: 'form-group-custom'
    }}
  >
    {Template.bind({})}
  </Story>
</Canvas>

## Props

<ArgsTable of={InputRadioGroup} />
