import InputRadio from './index';

<Meta
  title="Inputs/InputRadio"
  component={InputRadio}
  args={{
    value: '',
    onChange: () => {}
  }}
/>

export const Template = (args) => <InputRadio {...args} />;

# InputRadio

<WorksWith component="InputFactory" />

The `InputRadio` component is a simple radio input with a label.

<Canvas withSource="open">
  <Story
    name="Unchecked"
    args={{
      name: 'input-radio',
      label: 'Unchecked'
    }}
  >
    {Template.bind({})}
  </Story>
  <Story
    name="Checked"
    args={{
      name: 'input-radio - checked',
      label: 'Checked',
      checked: true
    }}
  >
    {Template.bind({})}
  </Story>
  <Story
    name="Disabled"
    args={{
      name: 'input-radio - disabled',
      label: 'Disabled',
      disabled: true
    }}
  >
    {Template.bind({})}
  </Story>
</Canvas>

## Props

The `InputRadio` has the same props as the html `<input type="radio">` element, along with the following additional props:

<ArgsTable of={InputRadio} />
