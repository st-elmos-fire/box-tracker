import {useState} from 'react';

import InputImage from './index';

<Meta
  title="Inputs/InputImage"
  component={InputImage}
  args={{
    name: 'image-picker'
  }}
/>

export const Template = (args) => <InputImage {...args} />;

# InputImage

<WorksWith component="InputFactory" />

The `InputImage` component is used to upload abd crip images. It is a wrapper around the `InputFile` component with some
additional functionality.

<Canvas withSource="open">
  <Story name="Image picker">
    {
      (args) => {
        const [image, setImage] = useState();
        return (
          <>
          <InputImage
            {...args}
            onUpload={setImage}
          />
          <div style={{
            display: 'block',
            margin: '20px 0',
            padding: '20px',
            border: '1px solid #ccc',
            textAlign: 'center'
          }}>
          {
            image ? (
              <img
                src={image}
                alt="Uploaded image"
                style={{
                  width: '100%'
                }}
              />
            ) : <span>Your image will appear here when Uploaded <br />(This appears in storybook only)</span>
          }
          </div>
          </>
        );
      }
    }
  </Story>
  <Story
    name="Image picker (disabled)"
    args={{
      disabled: true
    }}
  >
    {
      (args) => (
        <InputImage {...args} />
      )
    }
  </Story>
</Canvas>

## Props

The `InputImage` has the same props as the html `<input type="file">` element, along with the following additional props:

<ArgsTable of={InputImage} />
