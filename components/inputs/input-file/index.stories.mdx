import InputFile from './index';

<Meta
  title="Inputs/InputFile"
  component={InputFile}
  args={{
    name: 'file-picker'
  }}
/>

export const Template = (args) => <InputFile {...args} />;

# InputFile

<WorksWith component="InputFactory" />

The `InputFile` component is used to upload files. It is a wrapper around the `<input type="file" />` element.

<Canvas withSource="open">
  <Story name="File picker">{Template.bind({})}</Story>
  <Story
    name="File picker (disabled)"
    args={{
      disabled: true
    }}
  >
    {Template.bind({})}
  </Story>
</Canvas>

The InputFile componet can be multiple sizes, `small`, `medium` and `large`.

<Canvas withSource="open">
  <Story
    name="Small"
    args={{
      size: 'small'
    }}
  >
    {Template.bind({})}
  </Story>
  <Story
    name="Medium"
    args={{
      size: 'medium'
    }}
  >
    {Template.bind({})}
  </Story>
  <Story
    name="Large"
    args={{
      size: 'large'
    }}
  >
    {Template.bind({})}
  </Story>
</Canvas>

## Props

The `InputFile` has the same props as the html `<input type="file">` element, along with the following additional props:

<ArgsTable of={InputFile} />
