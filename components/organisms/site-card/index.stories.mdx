import { DndProvider } from 'react-dnd'
import { HTML5Backend } from 'react-dnd-html5-backend'

import { SiteCard } from "./index.tsx";

import boxes from 'lib/mocks/boxes'

<Meta
  title="Organism/Site card"
  component={SiteCard}
/>

<Title type="ALPHA">Site card</Title>

<Description of={SiteCard} />

<Canvas withSource="open">
 <Story
    name="Default"
    args={{
      name: "Meteor Street",
      address: "23 Meteor Street, Tuffnell Park, London, N7 90PW",
      type: "source",
      boxes
    }}
  >
    {(args) => <DndProvider backend={HTML5Backend}><SiteCard {...args} /></DndProvider>}
  </Story>
</Canvas>

## Props

<ArgsTable of={SiteCard} />
